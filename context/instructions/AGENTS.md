# Context Instructions

This directory contains machine-readable instruction context for the harness.

- Keep prose agent rules in `AGENTS.md` and `docs/agent/`.
- Keep files here concise, structured, and deterministic.
- Prefer JSON for data loaded by `harness.context.ContextLoader`.
- Do not duplicate long-form documentation here.


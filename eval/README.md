# Eval

Evaluation adapters for the deterministic harness live here.

`eval/local.py` performs local repository checks such as configured validation
commands, diff-size checks, unrelated-file detection, and report completeness.


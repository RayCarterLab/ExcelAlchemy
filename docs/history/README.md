# Historical Records

This directory contains archived repository records.

These files are useful for understanding past decisions and work plans, but
they are not current agent rules. Current agent rules live in `AGENTS.md` and
`docs/agent/*`.

- `docs/history/adr/`: historical architecture decision records.
- `docs/history/plans/`: archived hand-written implementation and release
  plans.

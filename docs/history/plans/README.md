# Archived Plans

This directory contains historical hand-written plans.

Current harness-generated plan artifacts belong in `plans/active/` or
`plans/archive/`. Archived plans here are retained only for historical context
and do not override `AGENTS.md` or `docs/agent/*`.
